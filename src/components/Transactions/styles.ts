import styled from "styled-components";

export const Container = styled.div`
    margin-top: 3rem;

    .table-light {
        color: var(--text-body);
        font-weight: 300;

        tbody {
            border-top-width: 0.09rem;
        }
    }
`;
