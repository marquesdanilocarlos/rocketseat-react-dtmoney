import styled from "styled-components";

export const Container = styled.div`
    margin-top: 3rem;

    .table-light {
        color: var(--text-body);
        border-spacing: 0.5rem;

        tbody,
        thead {
            font-weight: 300;
        }
    }
`;
